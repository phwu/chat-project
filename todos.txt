To dos (no particular order):
x persist chat messages
x fetch old chat messages (how should I display?)
# how to remove empty rooms (owner is gone and room is destroyed but how can i update the array?)
# ability to leave rooms and join others
# ui pretty
# authenticate the user
# support for pics ?
x clean up code obviously
# validate messages being sent (Sanitize, check for XSS)
x time stamps instead of dates